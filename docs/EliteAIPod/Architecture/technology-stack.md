---
project_id: a7a89076-0478-4718-85f7-88d51850f63c
stage: architecture
agent: solution_architect
deliverable: Technology-Stack
generated_at: "2026-08-11T10:30:09.217861+00:00"
---

# Technology Stack — tip-calculator

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-11T10:30+00:00.

| Category | Technology | Version | Rationale |
|---|---|---|---|
| backend | FastAPI | 0.111 | FastAPI provides automatic OpenAPI documentation, native async support, and clean Pydantic-based request/response validation. Its dependency injection system (Depends) maps directly to the repository pattern needed for T-002 and T-003. Startup time and throughput are well within the 10-concurrent-user scalability NFR. |
| database | PostgreSQL | 16 | PostgreSQL provides a robust relational store for the messages table defined in T-001, supports the DESC index on created_at for efficient reverse-chronological ordering, and is the standard target for Alembic migrations. Its reliability characteristics satisfy the 99.9% valid-request success NFR. |
| frontend | React | 18.3 | React's component model cleanly separates the MessageTimeline (T-004) and SubmissionForm (T-005) components. useState and useEffect cover all state management needs (loading/error/messages list, isSubmitting flag), satisfying US-003's optimistic prepend without a full page refresh. The textContent assignment pattern enforced in React JSX prevents XSS rendering. |
| orm | SQLAlchemy | 2.0 | SQLAlchemy 2.0 provides a type-safe, async-compatible ORM that pairs with FastAPI naturally. Its Core and ORM layers both support the simple messages schema from T-001 and integrate directly with Alembic for migrations. |
| migration | Alembic | 1.13 | Alembic is the standard migration tool for SQLAlchemy projects and is required to implement T-001 (schema migration script with messages table and created_at index). |

**backend — alternatives considered:** Flask — synchronous by default and requires additional libraries for validation and docs, Django REST Framework — too heavyweight for a two-endpoint demo app with no auth model
**database — alternatives considered:** SQLite — file-based, zero setup, but lacks concurrent write safety under the 10-user load target, MySQL — viable alternative but PostgreSQL has better JSON support and richer index options
**frontend — alternatives considered:** Vanilla HTML/JS — viable for this scope but JSX and component isolation make the XSS-safe rendering and isSubmitting flag easier to reason about, Vue 3 — comparable feature set but React has wider team familiarity and better ecosystem for this scope
**orm — alternatives considered:** Tortoise ORM — async-native but smaller ecosystem and less Alembic integration, Raw psycopg2 — avoids ORM overhead but loses migration tooling and query safety
**migration — alternatives considered:** Flyway — Java-based, less natural fit for a Python/FastAPI stack, Manual SQL scripts — error-prone and not reproducible across environments
