---
project_id: a7a89076-0478-4718-85f7-88d51850f63c
stage: architecture
agent: solution_architect
deliverable: System-Components
generated_at: "2026-08-11T10:30:11.496213+00:00"
---

# System Components — tip-calculator

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-11T10:30+00:00.

## GuestbookFrontend (service)

React 18 single-page application served as static files. Contains MessageTimeline and SubmissionForm components.

**Responsibilities:**
- Render message timeline by calling GET /api/v1/messages on page load (US-001 / T-004)
- Display loading spinner, empty state, and error state for the message list
- Render submission form with Name and Message fields and Post button (US-002 / T-005)
- Disable Post button until both fields have content and during in-flight requests
- Prepend newly created message to feed after successful POST response (US-003 / T-006)
- Display form error message on POST failure
- Render all message content via textContent (never innerHTML) to prevent client-side XSS
**Depends on:** GuestbookAPI**Communication protocol:** REST/HTTP
**Port:** 3000
## GuestbookAPI (service)

FastAPI backend providing REST endpoints for listing and creating guestbook messages. Organised into routes, services, and repositories layers.

**Responsibilities:**
- Expose GET /api/v1/messages returning all messages ordered by created_at DESC (T-002)
- Expose POST /api/v1/messages accepting name and message, validating and sanitizing input, persisting to database, and returning 201 with full message object (T-003)
- Strip HTML/script tags from name and message fields server-side before persistence (XSS NFR)
- Return structured error responses with safe messages for 400 and 500 status codes
- Enforce character-length constraints (name ≤ 100, message ≤ 2000)
**Depends on:** MessageDatabase**Communication protocol:** REST/HTTP
**Port:** 8000
## MessageDatabase (database)

PostgreSQL 16 relational database storing the messages table with columns id, name, message, and created_at.

**Responsibilities:**
- Persist guestbook messages with auto-increment primary key (T-001)
- Support reverse-chronological queries via DESC index on created_at
- Enforce NOT NULL constraints on name and message columns
**Communication protocol:** TCP
**Port:** 5432
