---
project_id: a7a89076-0478-4718-85f7-88d51850f63c
stage: architecture
agent: solution_architect
deliverable: API-Contracts
generated_at: "2026-08-11T10:30:13.751611+00:00"
---

# API Contracts — tip-calculator

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-11T10:30+00:00.

**Naming convention:** snake_case · **Versioning:** url_prefix

## Authentication & Authorization

- **Method:** none
- **Token format:** none
- **Token expiry:** 60 minutes
- **Refresh enabled:** no
- **Roles:** anonymous
- **Password hashing:** none
- **MFA supported:** no

**Role → endpoint mapping:**
- **anonymous:** /api/v1/messages

## Endpoints

| Method | Path | Auth | Summary |
|---|---|---|---|
| GET | `/api/v1/messages` | no | Retrieve all guestbook messages in reverse-chronological order |
| POST | `/api/v1/messages` | no | Create a new guestbook message |

### GET `/api/v1/messages`

Returns all messages from the database ordered by created_at DESC. Returns an empty array when no messages exist. Returns 500 with a safe error message on database failure.

**Status codes:** `200` OK — array of message objects (may be empty), `500` Internal Server Error — database failure, safe error message returned_Related stories: US-001_
### POST `/api/v1/messages`

Accepts name and message fields, validates both are present and non-empty, strips HTML/script tags server-side, persists to the database, and returns the full created message object with 201. Returns 400 for missing/empty fields, 500 for database errors.

**Status codes:** `201` Created — full message object (id, name, message, created_at), `400` Bad Request — name or message field missing or empty, `500` Internal Server Error — database failure, safe error message returned_Related stories: US-002_
